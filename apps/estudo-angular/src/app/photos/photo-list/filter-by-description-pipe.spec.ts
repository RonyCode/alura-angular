import { FilterByDescriptionPipe } from './filter-by-description-pipe';

describe('FilterByDescriptionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
