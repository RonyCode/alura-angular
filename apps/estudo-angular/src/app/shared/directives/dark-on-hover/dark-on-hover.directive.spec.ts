import { DarkOnHoverDirective } from './dark-on-hover.directive';

describe('DarkOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
